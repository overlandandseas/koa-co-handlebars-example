'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _queryString = require('query-string');

var _koaRouter = require('koa-router');

var _koaRouter2 = _interopRequireDefault(_koaRouter);

var _user = require('../../models/user');

var _user2 = _interopRequireDefault(_user);

var _utils = require('../../utils');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { return step("next", value); }, function (err) { return step("throw", err); }); } } return step("next"); }); }; }

const router = new _koaRouter2.default();

router.get('/reset-password', (() => {
  var ref = _asyncToGenerator(function* (ctx, next) {
    const queries = (0, _queryString.extract)(ctx.request.originalUrl);
    const _id = (0, _queryString.parse)(queries).name;

    if (!_id) {
      ctx.body = {
        error: 'No username'
      };

      return;
    }

    const query = _user2.default.where({ _id: _id });
    let user = false;

    try {
      user = yield query.findOne();
    } catch (err) {
      (0, _utils.log)('Couldn\'t load user', err);
    }

    if (!user) {
      ctx.body = {
        error: 'User doesn\'t exist'
      };

      return;
    }

    ctx.body = {
      success: 'LOL'
    };

    yield next();
  });

  return function (_x, _x2) {
    return ref.apply(this, arguments);
  };
})());

exports.default = router;
module.exports = exports['default'];